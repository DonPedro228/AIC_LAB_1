upstream flask_app {
    server web:5000;
}

server {
    listen 5000;

    location / {
        proxy_pass http://flask_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
